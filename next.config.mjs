/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Server Actions are enabled by default in Next 14. Add Supabase image domains
  // here if/when you render Storage images.
};

export default nextConfig;
